<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" >
    <!--<item android:state_focused="true" android:drawable="@drawable/feature_start_btn_normall"></item>
    <item android:state_pressed="true" android:drawable="@drawable/feature_start_btn_normall"></item>
    <item android:state_pressed="false" android:drawable="@drawable/feature_start_btn_pressed"></item>-->
    <!--属性：
       [html] view plaincopyprint?
       <shape>  android:shape=["rectangle" | "oval" | "line" | "ring"]
       其中rectagle矩形，oval椭圆，line水平直线，ring环形
       <shape>中子节点的常用属性：
       <gradient>  渐变
       android:startColor  起始颜色
       android:endColor  结束颜色
       android:angle  渐变角度，0从上到下，90表示从左到右，数值为45的整数倍默认为0；
       android:type  渐变的样式 liner线性渐变 radial环形渐变 sweep
       <solid >  填充
       android:color  填充的颜色
       <stroke > 描边
       android:width 描边的宽度
       android:color 描边的颜色
       android:dashWidth 表示'-'横线的宽度
       android:dashGap 表示'-'横线之间的距离
       <corners > 圆角
       android:radius  圆角的半径 值越大角越圆
       android:topRightRadius  右上圆角半径
       android:bottomLeftRadius 右下圆角角半径
       android:topLeftRadius 左上圆角半径
       android:bottomRightRadius 左下圆角半径-->
    <item android:state_pressed="true" >
        <shape>
            <solid android:color="#41a3f5"/>
            <corners android:radius="90dip"/>
            <size android:width="320dp" android:height="45dp" />
        </shape>
    </item>
    <item android:state_pressed="false">
        <shape >
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="#41a3f5"
                android:width="1dip"/>
            <corners android:radius="90dip"/>
            <size android:width="320dp" android:height="45dp" />
        </shape>
    </item>
</selector>
